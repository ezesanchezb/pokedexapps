import { createApp } from 'vue'; // Importamos la función createApp desde Vue.js
import App from './App.vue'; // Importamos el componente principal de Vue
import './index.css'; // Importamos los estilos CSS

// Creamos la aplicación Vue y montamos el componente principal en el elemento con el ID 'app'
createApp(App).mount('#app');

